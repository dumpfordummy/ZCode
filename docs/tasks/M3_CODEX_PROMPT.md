Continue the existing Graph Engineering repository. Implement M3 only.

Read first:
1. AGENTS.md and current docs/PROGRESS.md.
2. docs/ARCHITECTURE.md and docs/CONTRACTS.md.
3. docs/handoffs/M2_REPORT.md and docs/handoffs/M2_LEAD_REVIEW.md.
4. docs/tasks/M3_RUN_EXECUTION.md and docs/tasks/M3_DATA_BINDINGS.md.
5. docs/handoffs/M3_REPORT_TEMPLATE.md and docs/M3_SOURCES.md.

The lead conditionally accepts M2 for development progression and
explicitly authorizes M3 implementation against isolated fixtures.
A successful live text-probe screenshot has been supplied; the real
before/after-restart pair remains unconfirmed. Do not infer it, access
live credentials, or run that probe yourself. Preserve historical reports.
Update stale M2-only scope statements narrowly, not other repository rules.

Inspect the current working tree, instructions, and toolchain. Preserve
all existing/uncommitted work and run the M2 baseline before feature work.
Retain the corrected process-ownership guards. Inspect/test them using
retained handles and synthetic processes; never kill by process name or
parent-PID ancestry, and never experiment on Windows services.

State a short plan, agree versioned data/API/state contracts, then implement
end to end. Parallel workers may own disjoint files only after contracts
are fixed; one owner integrates and verifies the complete application.

Deliver explicit input/output bindings; a one-worker, SQLite-backed
Responses execution path; immutable run snapshots; bounded node attempts,
artifacts/events; idempotent run admission; cancellation; conservative
restart reconciliation; authenticated SignalR; and the Vue Runs view.
Keep the existing editor/provider security. Enable Run only for an
explicitly saved, locally ready graph and deliberate user submission.

Implement text and strict local JSON-object parsing, not provider schema
mode. No silent whole-history passing, automatic inference retries,
streaming, tools, shell commands, coding agents, branching, repair loops,
company-repository access, or M4 work. No normal-application mock success.

Use isolated SQLite data, actual Windows security facilities where required,
synthetic keys, and controllable HTTP providers. Test actual request counts,
real backend restart, profile-version changes, cancellation races, and
SignalR reconnect/auth via the dev proxy. Do not inspect Codex auth files,
live pairing files, or the user's credential database. Disable secret-bearing
recordings and do not capture real API credentials.

Complete the full verification matrix, review the final diff, and run the
integrated check after the last fixes. Write docs/handoffs/M3_REPORT.md,
update docs/PROGRESS.md, and provide exact startup and user-check steps.
Without a user-operated real workflow, mark it NOT RUN and report
READY FOR USER WORKFLOW CHECK only when required automated checks pass.
Report the M2 real restart check separately. Never invent PASS results.

Do not automatically stage, commit, push, merge, release, or begin M4.
