# Zn implementation report

Date/timezone:
Selected milestone:
Checkout / branch / starting HEAD / final HEAD:
Current working-tree changes preserved:
User's explicit scope instruction:

## Status

Choose IMPLEMENTED — READY FOR USER CHECK / IMPLEMENTED WITH NAMED BASELINE EXCEPTIONS / BLOCKED / INCOMPLETE. Do not label a task accepted merely because this template exists.

Implementation:
Required automated/native evidence:
Preexisting failures and comparison:
User-operated live checks:
Unsupported/remaining scope:

## Prerequisites and baseline

Previous milestone reports inspected and actual features confirmed:
Missing or pending live checks and why they do/do not block this isolated work:
Toolchain and source versions actually used:
Baseline commands, exit codes, PASS/FAIL/NOT RUN and artifacts:
Later authorized changes preserved:

## Contracts and source map

Spec changes:
State owners and identity/correlation:
Native services used (actual files/symbols, not invented APIs):
Versioning/migration/compatibility:
Permission, data and workspace boundaries:
Scope changes or deviations and their authorization:

## Implemented behavior

Describe only behavior actually implemented. Keep future plans separate.

## Verification matrix

| Requirement ID | Result | Layer | Exact command/action | Actual result | Evidence path |
|---|---|---|---|---|---|

Layers: unit/service fixture; native integration with controlled provider; user-operated real provider/project; packaged environment. A native task can make many model calls; distinguish input dispatch counts from request counts.

## Intermediate failures and corrections

Original failure/evidence:
Cause (verified versus inferred):
Correction and narrow changed files:
Regression rerun/result:
Remaining gaps:

Do not erase failures or claim a documented test plan was executed.

## Visual and operator evidence

Screenshots show actual native interface with synthetic/sanitized content.
Describe the exact session/input, artifact, approval, branch or iteration shown.
Record which screenshots are test-provider versus live-user evidence.

## User checks — separate

User authorization / performed by / date:
Provider alias only (no credential/private endpoint):
Approved workspace alias only:
Run/node/session/input IDs:
Observed actual tools and independent file/test evidence:
Restart/cancellation/approval results:
NOT RUN items:

## Diff and security review

Changed paths and purpose:
Existing work preserved:
No unexpected credentials/network/configuration/process changes:
Artifact/data-retention limitations:
Existing failing checks versus newly introduced failures:
Unverified claims avoided:

## Exact local reproduction and manual check

Use commands verified against this checkout, with prerequisites and safe isolated data.
Do not instruct broad process termination or installed-profile reset.

## Next eligible milestone

Name and prerequisite status only. Do not start it automatically.
Git staging/commit/push/publication performed (normally none):
