# Native Graph Engineering progress

Updated 2026-09-24, Asia/Kuala_Lumpur. This is the native ZCode feature's current progress; the supplied standalone M1–M4 planning history is not this implementation.

- Z1 native integration accepted by the lead for development progression. User screenshots establish greeting and native Chat navigation only. User-operated Read/Edit/test, permissions, cancellation and restart remain **NOT RUN**.
- Z1 Windows distribution `graph-v3.14.0-z1.2` and authorized Git history are preserved. Current source began at `cbb91b064aee711f6ccf597390d2bff4609ea749`, branch `main`, freshly synchronized with origin.
- Z2 is **READY FOR USER MULTI-AGENT CHECK** under [Z2_SPEC.md](Z2_SPEC.md). Editable sequential native tasks, explicit handoffs, frozen settings and inactive-only audited recovery are implemented. [Z2_REPORT.md](Z2_REPORT.md) records the complete verification matrix, screenshots, exact operator instructions and remaining limits.
- Final checks: 62 Graph/native source tests, 20 UI tests and 19 regression tests pass; root/CLI typechecks, root lint (70 warnings/0 errors), architecture and actual CLI/desktop builds pass. Controlled native three-task runs, dynamic handoffs, real Read/Edit/Bash plus independent tests, permission/question/cancellation, completed/interrupted/boundary restarts, audited inactive release/refusal, final native Chat/Z1 regressions and manual setup/reopen pass. These do not replace user live-provider evidence.
- Baseline exceptions remain visible: CLI lint has the same 53 warnings/85 errors with identical normalized diagnostics. Full formatting reports 2,876 paths versus 2,869 initially: eight later user-supplied future documents added and one changed source file removed from the failing list. Changed/new owned files pass scoped formatting. No suppression or repository-wide reformat was performed.
- User-operated Z2 three-task workflow remains **NOT RUN**. No company repository or installed credentials/settings are used. No paid/live-model task is performed by the implementation agent.
- The original Z2 implementation assignment excluded Git publication. On 2026-09-24 the user explicitly authorized committing the completed code and publishing the Windows prerelease; that release follow-up uses the existing Graph workflow and keeps unavailable live-user checks NOT RUN. Z3 was outside that release follow-up; the later explicit Z3 assignment is recorded below.
- **Published:** [ZCode Graph 3.14.0-z2.2 for Windows x64](https://github.com/dumpfordummy/ZCode/releases/tag/graph-v3.14.0-z2.2), from commit `55c5ad6d0c8d5dc464686ec4f1e1b7398cf2a1d3`. CI passed 137 source tests, typecheck, root lint/architecture, installer build and all eleven detached packaged scenarios. Installer/checksum assets were verified. Supplemental CLI lint and full formatting still fail as documented baseline exceptions. [PUBLISH_REPORT.md](PUBLISH_REPORT.md) records the actual release evidence; second-PC/install-upgrade and live-provider checks remain NOT RUN.

## Z3 explicit assignment

The user subsequently selected Z3 only, starting from `5ded9d1b6e9399e05f6ab6efc60234387322fe20`. [Z3_SPEC.md](Z3_SPEC.md) and [Z3_SOURCE_SPEC.md](Z3_SOURCE_SPEC.md) define durable entry/intermediate/final Human Approval controls, immutable bounded evidence, serialized decisions and explicit conservative restart continuation. The implementation preserves the published Z2 code/packaging and native session/Chat/configuration ownership.

All twelve controlled native Z3 scenarios passed, including actual tools, unchanged independent test, native questions/permissions, entry/intermediate pending restart, both decision/dispatch crash boundaries, failed metadata, reject/cancel with unrelated Chat, stale graph/source, and incomplete binary/oversized evidence. Z1/ordinary Chat and Z2 complete/permission-restart/recovery regressions also passed. Final UI/evidence and check details are recorded in [Z3_REPORT.md](Z3_REPORT.md).

User-operated live-provider/project checks, second-PC/installer checks and Z3 packaging/publication remain **NOT RUN**. The historical z2.2 installer is unchanged. No repository staging, commit, push, merge or publication was performed for Z3. Z4 was not started under that assignment; the later explicit authorization is recorded below.

## Z4 explicit assignment

The user selected Z4 only from the existing uncommitted Z3 checkout at `5ded9d1b6e9399e05f6ab6efc60234387322fe20`. Z3's actual implementation, report and sequential/gate/recovery prerequisites were verified before editing. [Z4_SPEC.md](Z4_SPEC.md), [Z4_NATIVE_SPEC.md](Z4_NATIVE_SPEC.md), [Z4_ARTIFACT_SPEC.md](Z4_ARTIFACT_SPEC.md) and [Z4_FIXTURE_SPEC.md](Z4_FIXTURE_SPEC.md) define the bounded extension. [Z4_REPORT.md](Z4_REPORT.md) records implementation, real evidence, exact commands, screenshots and operator instructions.

Z4 is **IMPLEMENTED WITH NAMED BASELINE EXCEPTIONS**: immutable artifacts and exact whole/JSON-field bindings, optional strict structured Agent outputs, editable project recipes, native deterministic Tool nodes, real Build/Test verification, exact-session conversation navigation and conservative cancellation/interruption are implemented. Tool-only graphs use the same native session/tool executor without a model provider or model input. No second agent engine or process supervisor is introduced.

All eighteen selected Z4 native scenarios pass, including actual synthetic C# Read/Edit/Build/Test with three passing assertions and an independent test, genuine failed tests despite model PASS, zero/missing/stale/wrong-source/wrong-build reports, invalid/oversized JSON without repair, Tool-to-Agent handoff, secret/export checks, targeted cancellation and actual crash/lost-ACK persistence boundaries. Ten Z1–Z3/ordinary Chat native regressions pass. Actual Chinese/light 760px desktop presentation passes; mobile Web remains NOT RUN. Source/fixture tests total 233 passes; root/CLI typechecks, root lint, architecture and actual CLI/desktop builds pass. CLI lint remains 85 errors/53 warnings with no new diagnostic identities; full formatting retains baseline failures. See the report for exact comparison and the three enlarged preexisting max-lines violations.

Live-user/provider/project, second-PC/installer and Z4 packaging/publication checks remain **NOT RUN**. Existing credentials, installed app settings, company repositories and paid model accounts were not used. The historical z2.2 installer and prior reports/evidence are preserved. No staging, commit, push, merge or publication was performed for Z4. Z5 was not started under that assignment; the user's later explicit Z5 selection is recorded below.

## Z5 explicit assignment

The user selected Z5 only from the existing uncommitted Z3/Z4 checkout at `5ded9d1b6e9399e05f6ab6efc60234387322fe20`. Z4's actual native Edit/Build/Test, invalid-output handling, crash recovery and ordinary Chat passed fresh isolated prerequisite checks. [Z5_SPEC.md](Z5_SPEC.md) defines typed exclusive routes, one finite repair region, exact iteration maps and native verification evidence, frozen budgets and conservative explicit continuation. [Z5_REPORT.md](Z5_REPORT.md) records the completed implementation, actual evidence, screenshots, baseline comparison and exact isolated manual steps.

Z5 is **IMPLEMENTED WITH NAMED BASELINE EXCEPTIONS — ready for user check**. All 20 required controlled native scenarios pass, covering true/false/default/invalid Conditions, real two-step repair and independent success, false reviewer PASS, finite iterations/admissions/deadline, no progress, unknown accepted repair, safe explicit continuation, stale evidence/source, cancellation/rejection with unrelated Chat, illegal topology and exact iteration conversation navigation. The final repair run uses six Agent inputs and six native Build/Test processes in 12 distinct root sessions in the captured workspace. Tests pass 0/3, then 2/3, then 3/3; its 25 controlled model requests comprise 19 execution and six auxiliary title requests. Final approval and completed-history restart pass without new work.

All 276 source/fixture tests pass. Root/CLI typechecks, root lint (70 warnings/0 errors), architecture and CLI/desktop builds pass. CLI lint remains exactly 85 errors/53 warnings; full formatting remains exactly the same 2,870 baseline paths. No rules were suppressed and changed/new owned files pass scoped formatting. Native Z4/Chat regressions, actual Chinese/light 760px desktop presentation and provider-free manual setup/reopen pass. Historical reports/evidence, CLI/protocol, settings ownership and z2.2 packaging are preserved; all 664 starting worktree entries remain present.

Live-user/provider/project checks, mobile/remote/non-Windows acceptance, second-PC/installer and Z5 packaging/publication remain **NOT RUN**. No installed credentials/settings, company repository or paid model account was used. No staging, commit, push, merge or publication occurred. **Z6 is next eligible but is not authorized or started.**

## Z6 explicit assignment

The user subsequently selected Z6 only and explicitly waived `Z6_REPORT.md`. That newer instruction supersedes the preceding historical eligibility statement. The starting checkout remains `5ded9d1b6e9399e05f6ab6efc60234387322fe20` with the existing Z3–Z5 work preserved. Fresh controlled native Z5 complete, decision-restart and ordinary Chat Read/Edit/test prerequisites passed before implementation; the initial renderer-crash attempt was retained with zero work and the scoped retry passed.

[Z6_SPEC.md](Z6_SPEC.md), [Z6_NATIVE_SPEC.md](Z6_NATIVE_SPEC.md) and [Z6_FIXTURE_SPEC.md](Z6_FIXTURE_SPEC.md) define a profile-local immutable workflow library, portable dry transfer, explicit local bindings and native configuration provenance. Versioned engineering, bounded bugfix and sequential slot templates are supplied in `templates/`. [Z6_SETUP.md](Z6_SETUP.md) describes the isolated manual workflow; [PRIVATE_PROJECT_PILOT_CHECKLIST.md](PRIVATE_PROJECT_PILOT_CHECKLIST.md) reserves company use for separate named authorization.

Z6 is **IMPLEMENTED WITH NAMED BASELINE EXCEPTIONS — ready for user check**. The native library supports create, immutable versions, duplicate, archive/restore, explicit instantiation and dry portable import/export. Runs pin their selected version, parameters, local bindings, graph revision and native configuration provenance. The existing Host recaptures the preflight before admission; changed references/configuration stop visibly, and Unknown destinations require an explicit operational decision. The native metadata preview reuses existing instruction/skill/plugin/hook/MCP resolution without installing, connecting or starting a session. Native Chat, session ownership, permissions, cancellation and conservative recovery remain in their existing owners.

All **307 source/fixture tests pass**, with zero skips. Required controlled native engineering and bugfix runs passed actual tools, Build/Test, independent unchanged C# tests, human gates and completed-history restart. Bugfix results progressed 0/3 → 2/3 → 3/3 within two repairs (six Agent inputs, six native Tool processes, 25 controlled model requests). The slot run used only supplied GameDoc R1–R5, approved interpretation before edits, ran Normal/Free sequentially, explicitly excluded Bonus/Respin and passed all 13 independent edge cases (six Agent inputs, two Tool processes, 26 controlled model requests). No math/RTP target was supplied: comparison is N/A, not PASS. Actual native skill/instruction use was observed in the generic run. See the [native evidence index](evidence/z6/native/index.json).

The final [native library check](evidence/z6/native/library-current/z6-library-summary.json) passed version pinning, archive/restore, safe transfer, dirty-draft acknowledgment, restart and Chinese/light 760px presentation with zero Agent inputs/model requests. It includes the correction for an unanswered required boolean previously labelled Excluded; the final UI shows Not selected/indeterminate until an explicit choice. Final provider-free fresh setup/reopen, ordinary Chat with actual Read/Edit/Bash and independent test, and Z4 complete/approval/artifact/restart regressions passed; see the [final preservation index](evidence/z6/native/final-preservation-index.json). Initial zero-work renderer/startup and stale-bundle harness failures remain separate from successful reruns.

Root/CLI typechecks, root lint (70 warnings/0 errors), architecture (zero violations) and actual CLI/desktop builds pass. CLI lint remains **85 errors/53 warnings** with the same 138 rule/file identities; five existing max-lines violations increased: protocol server 834→837, bootstrap plugins 1280→1282, adapter plugins 878→880, file-config 509→510 and marketplace 2480→2481. Whole-checkout formatting retains exactly the same **2,870** baseline paths. No rule was suppressed and no repository-wide formatting occurred. [Verification logs and comparisons](evidence/z6/checks/verification.json) retain those exceptions. All 1,082 starting worktree entries remain present; historical reports/evidence, existing configuration and packaging are preserved. HEAD and the empty staging area are unchanged.

Controlled fixture evidence is separate from live-model quality and company suitability. User/company, external integrations, installed-profile, second-PC/packaging and mobile/remote/non-Windows acceptance remain **NOT RUN**. No installed credentials/settings, company repository or paid model account was used. No Z6 report was created, as explicitly requested; no staging, commit, push, merge, publication or Z7 work occurred. Z7 is next eligible only after separate explicit authorization.

## Z7 and additional portability verification

The user subsequently authorized Z7 and waived predecessor re-verification and `Z7_REPORT.md`. The current implementation and original Z7-A01–A11 evidence are recorded in [Z7_SPEC.md](Z7_SPEC.md), [Z7_SETUP.md](Z7_SETUP.md) and [verification.json](evidence/z7/verification.json). Earlier milestone eligibility statements above are historical.

The additional user-requested Fork/Join export/import check is recorded as **Z7-A12 — FAIL**. A fresh [service-level audit](evidence/z7/portability/README.md) verifies that the current portable library only transfers the sequential definition; the separate parallel plan is not included. Branch topology, concurrency, Join policy and integration configuration cannot complete the requested round trip. Supported sequential imports and rejected unsupported payloads invoked zero native/Tool/workspace ports in the audit. A valid Fork/Join native UI round trip remains **NOT RUN** because its transfer contract and controls are absent. This is an implementation gap, not an environment prerequisite or a reason to claim the expanded Z7 acceptance complete.

The audit adds a reproducible diagnostic and evidence without changing production behavior, resetting existing changes or beginning Z8. User-operated/live-provider checks remain NOT RUN. The user has not authorized Git publication under this assignment; nothing was staged, committed, pushed or merged.
