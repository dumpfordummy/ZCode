# Native Graph Engineering progress

Updated 2026-09-24, Asia/Kuala_Lumpur. This is the native ZCode feature's current progress; the supplied standalone M1–M4 planning history is not this implementation.

- Z1 native integration accepted by the lead for development progression. User screenshots establish greeting and native Chat navigation only. User-operated Read/Edit/test, permissions, cancellation and restart remain **NOT RUN**.
- Z1 Windows distribution `graph-v3.14.0-z1.2` and authorized Git history are preserved. Current source began at `cbb91b064aee711f6ccf597390d2bff4609ea749`, branch `main`, freshly synchronized with origin.
- Z2 is **READY FOR USER MULTI-AGENT CHECK** under [Z2_SPEC.md](Z2_SPEC.md). Editable sequential native tasks, explicit handoffs, frozen settings and inactive-only audited recovery are implemented. [Z2_REPORT.md](Z2_REPORT.md) records the complete verification matrix, screenshots, exact operator instructions and remaining limits.
- Final checks: 62 Graph/native source tests, 20 UI tests and 19 regression tests pass; root/CLI typechecks, root lint (70 warnings/0 errors), architecture and actual CLI/desktop builds pass. Controlled native three-task runs, dynamic handoffs, real Read/Edit/Bash plus independent tests, permission/question/cancellation, completed/interrupted/boundary restarts, audited inactive release/refusal, final native Chat/Z1 regressions and manual setup/reopen pass. These do not replace user live-provider evidence.
- Baseline exceptions remain visible: CLI lint has the same 53 warnings/85 errors with identical normalized diagnostics. Full formatting reports 2,876 paths versus 2,869 initially: eight later user-supplied future documents added and one changed source file removed from the failing list. Changed/new owned files pass scoped formatting. No suppression or repository-wide reformat was performed.
- User-operated Z2 three-task workflow remains **NOT RUN**. No company repository or installed credentials/settings are used. No paid/live-model task is performed by the implementation agent.
- The original Z2 implementation assignment excluded Git publication. On 2026-09-24 the user explicitly authorized committing the completed code and publishing the Windows prerelease; that release follow-up uses the existing Graph workflow and keeps unavailable live-user checks NOT RUN. Z3 remains outside scope.
